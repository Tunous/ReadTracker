CREATE TABLE book (
    id INTEGER NOT NULL,
    title TEXT NOT NULL,
    numPages INTEGER NOT NULL,
    imageUrl TEXT,
    authors TEXT
);

insert:
INSERT INTO book(id, title, numPages, imageUrl, authors)
VALUES (?, ?, ?, ?, ?);

deleteAll:
DELETE FROM book;

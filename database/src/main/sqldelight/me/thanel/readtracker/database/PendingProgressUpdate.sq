CREATE TABLE pendingProgressUpdate (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    readProgressId INTEGER NOT NULL,
    page INTEGER
);

insertUpdate:
INSERT INTO pendingProgressUpdate(readProgressId, page)
VALUES(?, NULL);
